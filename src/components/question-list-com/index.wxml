<view class="question-list-container">
  <view class="content" bindtap="navigateQuestion">
    {{question.content}}
  </view>

  <view class="tags">
    <view class="tag" wx:for="{{tags}}" wx:key='index' bindtap="navigateTag" data-tag="{{item}}">
      #{{item}}
    </view>
  </view>
</view>